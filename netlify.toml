# Netlify Configuration for Go Happy Cab POC App
# This configures the Vite-based web demo (root directory)

[build]
  # Build the Expo app located in dispatch-app directory
  base = "dispatch-app"
  command = "npm install && npm run build:web"
  publish = "dist"

  # Environment variables for build
  [build.environment]
    # Convex deployment URL (production)
    EXPO_PUBLIC_CONVEX_URL = "https://colorful-wildcat-524.convex.cloud"

# Redirect rules for SPA (Single Page Application)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Optional: Custom headers for security
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "no-referrer-when-downgrade"
